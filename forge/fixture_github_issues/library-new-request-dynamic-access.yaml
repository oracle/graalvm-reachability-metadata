# Primary fixture-backed E2E scenario. Fixture setup masks the existing
# `com.google.http-client:google-http-client:1.42.2` metadata, tests, and stats
# in the isolated worktree so the library-new-request flow regenerates a real
# dynamic-access library. The local generated dynamic-access report for this
# version has 17 call sites, which keeps fixture runs shorter than the previous
# Logback Core target while still exercising the dynamic-access path.
number: 9101
title: "Add support for com.google.http-client:google-http-client:1.42.2"
state: OPEN
labels:
  - library-new-request
assignees: []
project:
  number: 30
  item_id: fixture-project-item-9101
  status: Todo
blocked_by: []
body: |
  Please add GraalVM reachability metadata and a meaningful test suite for
  `com.google.http-client:google-http-client:1.42.2`.

  The library is Google HTTP Client. A useful test should cover reflective
  field metadata, data model parsing/serialization, type utilities, and I/O
  helpers. The requested run should use the dynamic-access workflow; the
  existing baseline for this version has 17 dynamic-access call sites in the
  generated dynamic-access report.

  Maven coordinate:
  `com.google.http-client:google-http-client:1.42.2`
comments:
  - author: fixture-reporter
    created_at: "2026-05-27T22:54:53Z"
    body: |
      This is intended as the runnable fixture-backed E2E issue for the
      dynamic-access path. Keep generated tests if no dynamic-access call sites
      are found so the fixture still verifies routing, metrics, and publication
      handoff.
