# Primary fixture-backed E2E scenario. Fixture setup masks the existing
# `ch.qos.logback:logback-core:1.1.3` metadata, tests, and stats in the
# isolated worktree so the library-new-request flow regenerates a real
# dynamic-access-heavy library. The local generated dynamic-access report for
# this version has 35 call sites.
number: 9101
title: "Add support for ch.qos.logback:logback-core:1.1.3"
state: OPEN
labels:
  - library-new-request
assignees: []
project:
  number: 30
  item_id: fixture-project-item-9101
  status: Todo
blocked_by: []
body: |
  Please add GraalVM reachability metadata and a meaningful test suite for
  `ch.qos.logback:logback-core:1.1.3`.

  The library is Logback Core. A useful test should cover appenders, encoders,
  rolling policies, filters, status handling, and resource loading. The
  requested run should use the dynamic-access workflow; the existing baseline
  for this version has 35 dynamic-access call sites in the generated
  dynamic-access report.

  Maven coordinate:
  `ch.qos.logback:logback-core:1.1.3`
comments:
  - author: fixture-reporter
    created_at: "2026-05-27T22:54:53Z"
    body: |
      This is intended as the runnable fixture-backed E2E issue for the
      dynamic-access path. Keep generated tests if no dynamic-access call sites
      are found so the fixture still verifies routing, metrics, and publication
      handoff.
