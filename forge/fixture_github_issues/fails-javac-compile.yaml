number: 9103
title: "Fix javac failure for org.glassfish.grizzly:grizzly-http-servlet:2.1.11"
state: OPEN
labels:
  - fails-javac-compile
assignees: []
project:
  number: 30
  item_id: fixture-project-item-9103
  status: Todo
blocked_by: []
body: |
  Based on real closed issue #7466, fixed by merged PR #7469.

  The scheduled compatibility update for
  `org.glassfish.grizzly:grizzly-http-servlet:2.1.11` failed while running:

  `env GVM_TCK_LV=2.1.11 GVM_TCK_NATIVE_IMAGE_MODE=current-defaults ./gradlew clean compileTestJava -Pcoordinates=org.glassfish.grizzly:grizzly-http-servlet:2.1.2`

  Please update the existing Grizzly HTTP Servlet tests so they compile against
  the requested version while preserving the servlet, resource, and dynamic
  access coverage from the previous `2.1.2` test version.
comments:
  - author: fixture-reporter
    created_at: "2026-05-21T09:09:33Z"
    body: |
      Fixture issue for demonstrating `fails-javac-compile` routing from a real
      historical failure. Fixture setup masks `2.1.11` in the isolated worktree
      and makes `2.1.2` the latest entry before resolving current coordinates.
