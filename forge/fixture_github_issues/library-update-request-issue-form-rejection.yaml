number: 9110
title: "Improve coverage for org.example.forge:issue-form-fixture:1.0.0"
state: OPEN
labels:
  - library-update-request
  - library-new-request
assignees: []
project:
  number: 30
  item_id: fixture-project-item-9110
  status: Todo
blocked_by: []
body: |
  Fixture issue for the pre-claim issue-form gate. It carries two workflow
  labels, so the `single-workflow-label` rule rejects it before any claim and
  before a driver is selected.

  The rejection must comment the failed rule with the conflicting labels and
  close the issue, without applying `human-intervention` and without preserving
  a branch.
comments:
  - author: fixture-reporter
    created_at: "2026-08-25T08:00:00Z"
    body: |
      The coordinates are deliberately fictional: the run must never reach an
      artifact repository, because a rule decidable from the issue payload alone
      is decided first.
