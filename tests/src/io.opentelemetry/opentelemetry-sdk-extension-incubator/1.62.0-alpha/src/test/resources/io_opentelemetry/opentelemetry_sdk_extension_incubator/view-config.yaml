- selector:
    instrument_name: request.duration
    instrument_type: HISTOGRAM
  view:
    name: configured.request.duration
    description: configured description
    aggregation: explicit_bucket_histogram
    aggregation_args:
      bucket_boundaries:
        - 100.0
        - 200.0
    attribute_keys:
      - method
