/*
 * Copyright and related rights waived via CC0
 *
 * You should have received a copy of the CC0 legalcode along with this
 * work. If not, see <http://creativecommons.org/publicdomain/zero/1.0/>.
 */
plugins {
    id "org.graalvm.internal.tck"
}

String libraryVersion = tck.testedLibraryVersion.get()

dependencies {
    testImplementation "com.sun.xml.bind:jaxb-impl:$libraryVersion"
    testImplementation 'org.assertj:assertj-core:3.22.0'
}

tasks.withType(Test).configureEach {
    systemProperty "com.sun.xml.bind.v2.bytecode.ClassTailor.noOptimize", "true"
}

tasks.named("nativeTest") {
    runtimeArgs.add("-Dcom.sun.xml.bind.v2.bytecode.ClassTailor.noOptimize=true")
}

graalvmNative {
    binaries {
        test {
            buildArgs.addAll([
                    "-Dcom.sun.xml.bind.v2.bytecode.ClassTailor.noOptimize=true",
                    "--initialize-at-run-time=com.sun.xml.bind.v2.runtime.reflect.opt.Injector",
                    "--initialize-at-build-time=org.xml.sax.helpers.DefaultHandler,org.xml.sax.helpers.LocatorImpl"
            ])
        }
    }

    agent {
        defaultMode = "conditional"
        modes {
            conditional {
                userCodeFilterPath = "user-code-filter.json"
            }
        }
    }
}
