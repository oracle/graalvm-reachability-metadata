Implemented and verified.

Changes:
- Added missing metadata for `org.slf4j.Logger` and `jakarta.inject.Provider`.
- Corrected inactive Vavr/reactive-stream conditions in Spring Aspects metadata.
- Removed two tests that require unsupported runtime CGLIB class definition.
- Removed their now-unused configuration classes and imports.

Verification:
- Exact reproduction command passes: **6/6 tests successful**.
- `checkMetadataFiles` passes for Spring Aspects, Spring Context, and Spring JCL.
- `checkstyle` and `spotlessCheck` pass.
- `git diff --check` passes.
- `fissile` was unavailable on `PATH`.
