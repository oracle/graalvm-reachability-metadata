Updated `Spring_aspectsTest.java` with two public BeanFactory API-path tests that exercise:

- `SpringConfiguredConfiguration.beanConfigurerAspect()`
- `AspectJTransactionManagementConfiguration.transactionAspect()`

The tests avoid runtime CGLIB configuration-class generation while still driving reflective constructor and factory-method access for metadata generation.

No metadata or build files were changed. Per instruction, tests were not run. `git diff --check` passes.
