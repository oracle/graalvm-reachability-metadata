Implemented and verified the fix.

Root cause:
- Initial Testcontainers lambda reflection error masked the underlying failure.
- Full tracing revealed Spring Boot expected AOT-generated application classes unavailable in the native JUnit test.
- Rewrote the test to exercise `VaultConfigDataLoader` directly through its public API.
- Replaced Testcontainers with a native-compatible Docker process.
- Regenerated and split library/test-only metadata.

Validation passed:
- Exact reproduction command: 1 test successful, 0 failed
- `./gradlew test -Pcoordinates=org.springframework.cloud:spring-cloud-vault-config:5.0.2`
- `checkMetadataFiles`
- `checkstyle`
- `spotlessCheck`
- `grund check`
