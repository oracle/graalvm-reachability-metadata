Fixed all Checkstyle violations in the affected test files, including the initially truncated `ObjectUtilsTest.java` errors.

Validation passed:

```text
./gradlew checkstyle -Pcoordinates=org.testcontainers:testcontainers:1.19.8
BUILD SUCCESSFUL
```

Pre-existing metadata and report changes were left untouched.
