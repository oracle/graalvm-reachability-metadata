Done.

Wrote report to:
`post-gen-interventions/org.objenesis_objenesis_1.2.md`

Conclusion:
- All 3 remaining failures are **metadata-related**
- Root cause is the still-unresolved reflection registration for `java.lang.Object` no-arg constructor on the `SunReflectionFactoryInstantiator` path
- **No tests were removed** since removing them would hide a real metadata gap, not a non-metadata issue
