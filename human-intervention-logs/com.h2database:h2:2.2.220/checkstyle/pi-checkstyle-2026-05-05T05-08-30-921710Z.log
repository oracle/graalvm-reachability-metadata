=== Pi checkstyle attempt 1 ===
Warning: Model "gpt-5.5" not found for provider "OCA". Using custom model id.
Fixed the checkstyle violations in:

- `tests/src/com.h2database/h2/2.2.220/src/test/java/h2/UtilsTest.java`

Replaced the modified `for` loop control variable updates with a `skipNextConstantPoolEntry` flag to preserve constant-pool parsing behavior without violating `ModifiedControlVariable`.

Validation passed:

```bash
./gradlew checkstyle -Pcoordinates=com.h2database:h2:2.2.220
```

Build result: `BUILD SUCCESSFUL`
