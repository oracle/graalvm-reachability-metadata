Root cause: metadata-related. Native Image is missing conditional serialization metadata for `org.junit.platform.engine.UniqueId$SerializedForm`; the metadata fix could not run in the read-only environment.

No tests or metadata were removed. `TempDirectoryTest` should be preserved.

I could not create the requested report because filesystem write and escalation were rejected.
