I’m blocked by the read-only workspace and denied execution approval:

- Exact GraalVM is present, but `native-image --version` cannot create its temp directory.
- The metadata patch was rejected because file writes are unavailable.
- No files were modified.

Please enable workspace writes and approve the exact-GraalVM Gradle command so I can complete and verify the fix.
