No hand-written metadata was added.

The reporter context contains no request (`_No response_`), so there is no mandatory missing entry to add. The generated metadata is valid JSON and was left unchanged, along with the existing test and filter changes.
