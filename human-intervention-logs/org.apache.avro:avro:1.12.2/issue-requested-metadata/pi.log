Added one missing hand-written entry to:

`metadata/org.apache.avro/avro/1.12.2/reachability-metadata.json`

It registers the `ReflectionUtil.getConstructorAsFunction` lambda, conditioned on:

```json
"typeReached": "org.apache.avro.reflect.ReflectionUtil"
```

The JSON parses successfully. Tests were not run, as requested. `fissile` was unavailable in the environment.
