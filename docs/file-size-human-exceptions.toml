fissile_exceptions_version = 2
[[exceptions]]
path = "forge/docs/architecture/code-coverage/workflow.md"
match = "exact"
rules = ["markdown-docs"]
kind = "deferred"
max_accepted = { value = 1000, unit = "lines" }
until = "the code coverage spec splits behavior into the functional spec"
reason = """
User-approved: behavior and architecture share one document until the promised FS split lands; docs/README.md records the same debt.
"""
[[exceptions]]
path = "forge/utility_scripts/code_coverage_profile_report.py"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 2000, unit = "lines" }
until = "the file is split when next substantively edited"
reason = """
User-approved: pre-existing size debt surfaced by fissile adoption; this branch touches the file minimally.
"""
[[exceptions]]
path = ".github/scripts/forge_pr_publisher/publisher.py"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1700, unit = "lines" }
until = "the file is split when next substantively edited"
reason = """
User-approved: pre-existing size debt surfaced by fissile adoption; this branch touches the file minimally.
"""
[[exceptions]]
path = "forge/benchmarks/code_coverage_benchmark.py"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1700, unit = "lines" }
until = "the file is split when next substantively edited"
reason = """
User-approved: pre-existing size debt surfaced by fissile adoption; this branch touches the file minimally.
"""
[[exceptions]]
path = "forge/tests/test_code_coverage_benchmark.py"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 800, unit = "lines" }
until = "the file is split when next substantively edited"
reason = """
User-approved: pre-existing size debt surfaced by fissile adoption; this branch touches the file minimally.
"""
[[exceptions]]
path = "forge/tests/test_code_coverage_profile_report.py"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1200, unit = "lines" }
until = "the file is split when next substantively edited"
reason = """
User-approved: pre-existing size debt surfaced by fissile adoption; this branch touches the file minimally.
"""
[[exceptions]]
path = "forge/utility_scripts/code_coverage_finalize.py"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 900, unit = "lines" }
until = "the file is split when next substantively edited"
reason = """
User-approved: pre-existing size debt surfaced by fissile adoption; this branch touches the file minimally.
"""
[[exceptions]]
path = "forge/tests/test_code_coverage_api_rank.py"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 700, unit = "lines" }
until = "the file is split when next substantively edited"
reason = """
User-approved: pre-existing size debt surfaced by fissile adoption; this branch touches the file minimally.
"""
[[exceptions]]
path = "forge/utility_scripts/code_coverage_api_rank.py"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 600, unit = "lines" }
until = "the file is split when next substantively edited"
reason = """
User-approved: pre-existing size debt surfaced by fissile adoption; this branch touches the file minimally.
"""
